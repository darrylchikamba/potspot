{
    "buildCommand": "cd client && npm install && chmod +x node_modules/.bin/vite && npm run build",
    "outputDirectory": "client/dist",
    "installCommand": "echo 'skip root install'",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}